#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/jx" ]; then
  "$basedir/jx"  "$basedir/../ws/bin/wscat" "$@"
  ret=$?
else 
  jx  "$basedir/../ws/bin/wscat" "$@"
  ret=$?
fi
exit $ret
